node_modules/
# anchored: unanchored `niti` matched any path component, which silently ignored
# tui/cmd/niti/ — the Go TUI's main package — so a clean clone could not build it.
/niti
/niti-core
/npm/
desktop/dist/
*.tgz
*.log
.DS_Store
.niti/agents.yaml
.niti/session.json
.niti/niti.db*
.niti/reports/
.env
.env.*
