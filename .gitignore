node_modules/
# anchored: unanchored `amux` matched any path component, which silently ignored
# tui/cmd/amux/ — the Go TUI's main package — so a clean clone could not build it.
/amux
/amux-core
/npm/
*.tgz
*.log
.DS_Store
.amux/agents.yaml
.amux/session.json
.amux/amux.db*
.amux/reports/
.env
.env.*
